## Backend DX Delta

> Использовать только для `BDX-L1..L3` в project `docs/project-context.md`.
> Для `BDX-L0` раздел не создавать. Удалить instructional comments после заполнения.

- Applicability level: `BDX-L1 | BDX-L2 | BDX-L3`
- Supported local environments:
- Canonical working directory:
- Toolchain/runtime versions:
- Package manager and lockfile:
- Canonical commands:
  - bootstrap:
  - doctor:
  - dev:
  - stop:
  - check:
  - test-fast:
  - test-integration: `N/A — reason` if not applicable
  - build: `N/A — reason` if not applicable
  - logs:
- Required local services: `N/A — reason` if none
- Readiness/status command: `N/A — reason` if no local services
- Ports and collision policy: `N/A — reason` if no listening service
- Config source, profiles and required variables:
- Secret redaction/effective-config diagnostics:
- API docs/spec and generated-contract drift command: `N/A — reason` if no API/generated contract
- DB migration/status/seed/reset-local commands: `N/A — reason` if no database
- Destructive command guard: `N/A — reason` if no destructive local/test command
- Worker/scheduler commands: `N/A — reason` if not applicable
- External sandbox/stub/fallback modes: `N/A — reason` if no external provider
- Clean-room smoke command or documented manual scenario:
- Project-specific quality gates:
- Known limitations:
- Explicit deviations from global Backend DX Policy: `none` or documented reason

Материальные требования сопоставь с `BDX-*` IDs и evidence по
`~/.codex/rules/backend-dx.md`. Не копируй global policy в project repository.
