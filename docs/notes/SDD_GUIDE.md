# SDD в AI Dev Team

## Источник истины

SPEC описывает стабильное наблюдаемое поведение продукта. Prompt описывает одноразовую работу агента. Код реализует SPEC, а тесты подтверждают соответствие.

Рабочая цепочка:

`Requirement ID → SPEC → selected STAGES record → код → тест → review → проверенная интеграция`

Selected record в `docs/STAGES.md` обязан ссылаться на идентификаторы требований и задавать ограниченную область, non-goals, проверки, изменения документации, Definition of Done и условие остановки. Он не является альтернативным источником требований.

## Структура проекта

```text
AGENTS.md
docs/
  ARCHITECTURE.md
  DESIGN.md
  DECISIONS.md
  ROADMAP.md
  SECURITY.md        # при наличии рисков безопасности
prompts/
  STAGES.md
specs/
  README.md
  system.spec.md
  features/
    <feature>.spec.md
```

`docs/STAGES.md` уже содержит компактное текущее состояние, поэтому отдельные `AI_STATUS.md`, `AI_PLAN.md`, `PROJECT_SNAPSHOT.md` или эквиваленты не нужны.

## Когда нужна SPEC

- `SIMPLE`: не создаётся автоматически; существующая SPEC обновляется при изменении существенного поведения.
- `STANDARD`: обязательна для новой функциональности.
- `COMPLEX`: полная SPEC, архитектурная проверка, анализ рисков и строгая validation обязательны.
- Prototype: допускается краткая экспериментальная SPEC с явными допущениями.

## Трассировка

Используй стабильные идентификаторы `FR-###`, `NFR-###`, `SEC-###`, `PERF-###` и `AC-###`. В тесте или таблице соответствия указывай, какие требования он покрывает.

Для существенного проекта используй компактную таблицу:

| Требование | Этап / STAGES | Реализация | Тесты | Статус |
|---|---|---|---|---|
| `FR-001` | | | | |

## Контекст

Hook загружает только `specs/README.md` и `specs/system.spec.md`. Конкретную feature-SPEC агент выбирает по текущей задаче. Это предотвращает загрузку всех спецификаций.
